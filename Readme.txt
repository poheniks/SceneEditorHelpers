For use in Custom Battles & the scene editor test mission (SimpleMountedPlayerMission).

Note the script component `SCE_EditorSpawn` is now implemented as a vanilla behavior using the tag system. Sceners can set the spawn location in the editor test mission either by assigning any entity with the script `SCE_EditorSpawn` or adding the tag `sp_play` 
